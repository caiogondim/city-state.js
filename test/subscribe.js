/* eslint-env jest */

const { Subscribe } = require('./index')

// it('re-render children whenever observable changes', () => {

// })

it('accepts multiple observables', () => {
  expect(1).toEqual(1)
})
