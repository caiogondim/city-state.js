- redux on example
- merge observables on devtools
- async example
  - loading/spinner
  - inside and outside
    showSpinner()
    await loadFromApi()
    hideSpinner()

    state = {
      isFetching: true,
      data: []
    }
